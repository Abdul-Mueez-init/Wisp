<?xml version="1.0" encoding="utf-8"?>
<!-- Wisp launch screen (wisp_fixes.txt item 2 permanent fix — see doc
     comment in main.dart's _SplashScreen for why this file, not
     pubspec.yaml, is what actually controls the true native launch
     screen shown before the Flutter engine draws its first frame).
     Background color matches AppColors.backgroundBase (#0D3A35,
     design.md's "Level 0 (Base)" surface) exactly, so there is zero
     visible color seam between this native screen and the Flutter
     _SplashScreen that immediately follows it. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launch_background_color" />
    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/launch_image" />
    </item>
</layer-list>
